# Project Instructions

- Current Godot mainline project: `first-demo(0.1.0)/`.
- Treat `first-demo/` as an old reference demo and `malegema/` as historical Python prototype code.
- Every code, scene, asset, configuration, or documentation change must be recorded in `CHANGELOG.md` in the same turn.
- Keep changelog entries dated and grouped by version or an explicit unreleased section.
- When validation is skipped or blocked, record the reason in the changelog entry.
